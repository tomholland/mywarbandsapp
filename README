For initial setup, run the following commands from inside the project's root directory:

sudo npm update -g cordova

mkdir {hooks,merges,platforms,plugins}

cordova platform add ios

cp icons_backup/* platforms/ios/MyWarbands/Images.xcassets/AppIcon.appiconset/ && cp splash_backup/* platforms/ios/MyWarbands/Images.xcassets/LaunchImage.launchimage/

cordova plugins add cordova-plugin-inappbrowser cordova-plugin-file cordova-plugin-file-transfer cordova-plugin-keyboard-ams https://github.com/ohh2ahh/AppAvailability.git https://github.com/katzer/cordova-plugin-hidden-statusbar-overlay cordova-plugin-dialogs cordova-plugin-email cordova-open

cordova build